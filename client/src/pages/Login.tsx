import {FormEvent,useState} from 'react';import {useAuth} from '../store/auth';
export default function Login(){const {login}=useAuth();const [email,setEmail]=useState('admin@demo.local');const [password,setPassword]=useState('Password123!');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await login(email,password)}catch{setError('Invalid credentials or server unavailable.')}finally{setBusy(false)}};return <div className="login"><div className="login-art"><div className="orb orb1"/><div className="orb orb2"/><div className="login-copy"><span className="eyebrow">VELO PROJECT OPS</span><h1>Everything your delivery team needs, in one live workspace.</h1><p>Projects, tasks, activity and team presence—connected in real time.</p><div className="feature-list"><span>✓ Role-aware access</span><span>✓ WebSocket activity</span><span>✓ PostgreSQL-backed history</span></div></div></div><form className="login-card" onSubmit={submit}><div className="brand large"><span className="brandmark">V</span><div><b>VELO</b><small>PROJECT OPS</small></div></div><h2>Welcome back</h2><p>Sign in to your workspace.</p><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email"/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password"/></label>{error&&<div className="error">{error}</div>}<button className="primary" disabled={busy}>{busy?'Signing in…':'Sign in'}</button><div className="demo">Demo accounts use password <b>Password123!</b></div></form></div>}
