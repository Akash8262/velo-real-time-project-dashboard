import {useState} from 'react';import Login from './pages/Login';import Dashboard from './pages/Dashboard';import Projects from './pages/Projects';import ProjectDetail from './pages/ProjectDetail';import {Layout} from './components/Layout';import {useAuth} from './store/auth';
export default function App(){const {user,loading,logout}=useAuth();const [page,setPage]=useState<'dashboard'|'projects'|'project'>('dashboard');const [projectId,setProjectId]=useState('');if(loading)return <div className="loading">Starting workspace…</div>;if(!user)return <Login/>;return <Layout onLogout={logout}><nav className="topnav"><button className={page==='dashboard'?'selected':''} onClick={()=>setPage('dashboard')}>Overview</button><button className={page!=='dashboard'?'selected':''} onClick={()=>setPage('projects')}>Projects</button></nav>{page==='dashboard'?<Dashboard/>:page==='projects'?<Projects onOpen={id=>{setProjectId(id);setPage('project')}}/>:<ProjectDetail id={projectId} onBack={()=>setPage('projects')}/>}</Layout>}
