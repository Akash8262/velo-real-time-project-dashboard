import {Router} from 'express'; import {auth} from '../middleware/auth'; import {prisma} from '../utils/prisma'; const r=Router();r.use(auth);r.get('/',async(req:any,res,next)=>{try{const notifications=await prisma.notification.findMany({where:{userId:req.user.id},orderBy:{createdAt:'desc'},take:50});const unread=notifications.filter(n=>!n.read).length;res.json({notifications,unread})}catch(e){next(e)}});r.patch('/:id/read',async(req:any,res,next)=>{try{const n=await prisma.notification.updateMany({where:{id:req.params.id,userId:req.user.id},data:{read:true}});res.json({ok:n.count>0})}catch(e){next(e)}});r.post('/read-all',async(req:any,res,next)=>{try{await prisma.notification.updateMany({where:{userId:req.user.id,read:false},data:{read:true}});res.json({ok:true})}catch(e){next(e)}});export default r;
